# Chaves do Firebase do SEU projeto — não sobem pro Git. Cada pessoa que
# clonar o repositório cria seu próprio projeto no Firebase e copia
# javascript/firebase-config.example.js para javascript/firebase-config.js,
# preenchendo com as chaves dela.
javascript/firebase-config.js

# Arquivos comuns de SO / editor que não têm nada a ver com o projeto.
.DS_Store
Thumbs.db
*.log
.vscode/
